#!/bin/sh
# Reload the 1Password SSH agent socket LaunchAgent whenever the plist changes.
# Hash below ties this script's identity to the plist content so chezmoi reruns it on change:
# plist sha256: {{ include "private_Library/LaunchAgents/com.1password.SSH_AUTH_SOCK.plist" | sha256sum }}

set -e
PLIST="$HOME/Library/LaunchAgents/com.1password.SSH_AUTH_SOCK.plist"
launchctl unload "$PLIST" 2>/dev/null || true
launchctl load "$PLIST"
